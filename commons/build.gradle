plugins {
    id 'io.emailradar.java-conventions'
}

group = 'io.emailradar'
version = '0.0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

ext {
    mime4jVersion = '0.8.7'
}

dependencies {
    implementation group: 'org.apache.james', name: 'apache-mime4j-dom', version: mime4jVersion
}